<div class="pages page-{{page}}">
  <ul class="select">
    {{#each pages}}
    <li data-id="{{id}}" {{#if active}}class="active"{{/if}}>{{name}}</li>
    {{/each}}
  </ul>
  <div class="page">
    <form>
      <a class="refresh"><i class="fa fa-refresh"></i> Last 6 Hours</a>
    </form>
    <div class="events-container">
    </div>
  </div>
</div>

