<ul class="alerts">
  {{#each alerts}}
  <li class="level-{{level}}" data-id="{{id}}">
    <i class="fa fa-warning"></i> {{description}}
  </li>
  {{/each}}
</ul>

<ul class="conditions">
  {{#each conditions}}
  <li>
    <div class="name">{{name}}</div>
    <div class="value">{{{value}}}</div>
    <div class="sub">{{sub}}</div>
  </li>
  {{/each}}
</ul>
