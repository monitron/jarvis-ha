<div class="pages page-{{page}} {{#if fullSize}}full-size{{/if}}">
  <ul class="select">
    {{#each pages}}
    <li data-id="{{id}}" {{#if active}}class="active"{{/if}}>{{name}}</li>
    {{/each}}
  </ul>
  <div class="page">
  </div>
</div>

